enum Event
{
    // Bounds
    Bounds,
    MinBounds,

    // Size
    Padding,
    Margin,
    MinSize,
    MaxSize,
    StretchRatio,

    // Position
    Position,
    Alignment,

    // Components
    Components,
    Parent,

    // Miscellaneous
    Visibility,
    Percentage,
    Player,
    Texture,
    FrameIndex,
    FrameDim,
    Crop,
    FixedAspectRatio,
    Clickable,
    Text,
    Font,
    Color,
    Wrap,
    MaxLines,
    HandleRatio,
    Checked,
    Spacing,
    CellWrap,
    FlowDirection,
    ScrollIndex,

    // Interaction
    Press,
    Release,
    Click,
    StartDrag,
    EndDrag,
    Scroll,
}
